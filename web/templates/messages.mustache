
    {{#each messages}}

    <div class="col-md-12 message" data-rel="{{id}}">
<span class="author">
  {{#with user}}<strong><a href="">{{name}}</a></strong>{{/with}}
</span>
    <span class="body">{{body}}</span>
    {{#if comments}}
    <span class="number-of-comments">Antall kommentarer: <span class="badge">{{length comments}}</span></span>
    
    {{/if}}
    </div>
    {{#if comments}}
    <div class="comments">
    {{#each comments}}
    <div class="comment">
    <span class="author">
  {{#with user}}<strong><a href="">{{name}}</a></strong>{{/with}}
</span>
<span class="body">{{text}}</span>
<hr/>


    </div>
    {{/each}}
    {{/if}}
    </div>
<hr/>
    {{/each}}
